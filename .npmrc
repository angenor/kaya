# Versions exactes obligatoires (principe XI, porte P-20).
# `save-exact` empêche pnpm d'écrire un `^` en ajoutant une dépendance : la porte P-20 le
# rattraperait, mais après coup — autant qu'il ne soit jamais écrit.
save-exact=true
