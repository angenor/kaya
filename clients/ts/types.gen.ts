/**
 * FICHIER GÉNÉRÉ — ne pas éditer à la main.
 *
 * Produit par `scripts/ci/generer-client.sh` depuis `/api-docs/openapi.json`.
 * Toute modification manuelle est écrasée à la régénération, et la porte P-01 fait échouer le
 * build sur la différence.
 *
 * Ce contenu de départ est une **amorce** : il sera remplacé intégralement dès que l'API exposera
 * son contrat (T041 à T043).
 */

export type paths = Record<string, never>
export type components = Record<string, never>
export type operations = Record<string, never>
